# 🌌 Antigravity - Source Matrix (GEMINI.md)

**Project Scope**: Source code for the Antigravity Agent Configuration Framework.
**Tech Stack**: Bash, Markdown, Agentic Protocols (Protocol Zero).

> [!WARNING]
> To any AI reading this: You are currently looking at the **Matrix Source Repository** (`01-antigravity`). 
> This is NOT an active web development project. This repository CONTAINS the rules and skills that are distributed to other projects.

## 📐 Local Architecture Rules

1. **Self-Referential Constraint**: Any modifications to `rules/` or `skills/` within this repository are changes to the framework itself, not local project constraints.
2. **Structural Sanctity**: The layout `rules/`, `skills/`, `workflows/`, `template/` must stay un-nested at the root directory because `install.sh` depends on them.
3. **No Local State**: Do NOT commit temporary agent memory directories (like `.agent` or `.gemini`) back into this repository. It must remain pure and stateless.

## 🤝 Protocol Enforcement

All edits must adhere to `Protocol 9: CommStyle` (Objective, no pleasantries) and `Protocol 13: Continuous Commit` (Immediate SCM sync).
